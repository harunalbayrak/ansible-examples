[DEFAULT]
verbose = True
debug = True
log_dir = /var/log/safir_gocmen
transport_url = rabbit://stackrabbit:secretrabbit@{{ host_ip }}:5672/

[keystone_authtoken]
memcached_servers = localhost:11211
cafile = /opt/stack/data/ca-bundle.pem
project_domain_name = Default
project_name = service
user_domain_name = Default
password = secretservice
username = safir_gocmen
auth_url = http://{{ host_ip }}/identity
interface = public
auth_type = password

[database]
connection = mysql://root:secretdatabase@127.0.0.1/safir_gocmen
